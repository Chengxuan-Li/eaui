# Claude Code instructions

@AGENTS.md

This file only imports the repository's agent rules; keep durable instructions in `AGENTS.md`, not here.

Commits and repository files must not carry assistant, model, or vendor attribution (no `Co-Authored-By` or similar trailers), even where a tool's default says otherwise.
